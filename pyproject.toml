[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "prediction_potTotal"
version = "0.0.1"
description = "Predicion de ventas de una jardineria"
authors = [
  { name = "Paola Soto" },
]

readme = "README.md"
classifiers = [
    "Programming Language :: Python :: 3",
    
]
requires-python = "~=3.10.0"


[tool.ruff]
line-length = 99
src = ["prediction_potTotal"]
include = ["pyproject.toml", "prediction_potTotal/**/*.py"]

[tool.ruff.lint]
extend-select = ["I"]  # Add import sorting

[tool.ruff.lint.isort]
known-first-party = ["prediction_potTotal"]
force-sort-within-sections = true

